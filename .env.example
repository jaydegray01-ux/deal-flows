# Local development only. Do NOT commit real secrets.
# Copy to .env and fill in values.

# Postgres connection string (example):
# postgres://USER:PASSWORD@localhost:5432/dealflow
DATABASE_URL=

# Any long random string (>= 32 chars)
SESSION_SECRET=dev-secret-change-me

# Optional: set to 5000 to match existing setup
PORT=5000
